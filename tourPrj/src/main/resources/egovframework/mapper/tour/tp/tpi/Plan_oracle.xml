<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="PlanDAO">

	<select id="selectMaxPlanId" resultType="java.lang.String">
 		
		SELECT LPAD(NVL(MAX(TOUR_PLAN_NO),0)+1,8,0) AS TOUR_PLAN_NO  FROM TB_TOUR_PLAN
 		
 	</select>
	
	<insert id="insertTourPlan">
	
		INSERT INTO ITEYES.TB_TOUR_PLAN(
			TOUR_PLAN_NO
			,TOUR_NM
			,TOUR_START
			,TOUR_END
			,FRST_REG_ID
			,FRST_MDFR_DT
			,LAST_REG_ID
			,LAST_MDFR_DT
			)
		VALUES(
			#{tourPlanNo}
			,#{tourNm}
			,#{tourStart}
			,#{tourEnd}
			,#{frstRegId}
			,SYSDATE
			,#{lastRegId}
			,SYSDATE
		)
	
	</insert>
	
</mapper>